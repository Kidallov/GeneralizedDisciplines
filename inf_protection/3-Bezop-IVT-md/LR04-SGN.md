# Упражнение 4.
## АСИММЕТРИЧНАЯ КРИПТОГРАФИЯ: протоколы электронной подписи и протоколы аутентификации на базе RSA

> *Главное — это из множества проблем выбрать наиболее простые, решение которых позволит выработать концепции, допускающие обобщения.*
> *Д.Гильберт*

---

## ОБЯЗАТЕЛЬНЫЕ РЕЗУЛЬТАТЫ ОБУЧЕНИЯ

### Предварительные понятия:
- криптосистема с открытым ключом (асимметричная криптосистема);
- криптографический протокол;
- однонаправленная функция;
- односторонняя функция с "лазейкой".

### Основные понятия:
- электронная подпись RSA без секретной передачи сообщения;
- электронная подпись RSA без секретной передачи сообщения (с хэшированием);
- электронная подпись RSA совместно с секретной передачей сообщения;
- аутентификация: протокол аутентификации.

### Уметь реализовать протоколы:
- электронной подписи RSA без секретной передачи сообщения;
- электронной подписи RSA без секретной передачи сообщения (с хэшированием);
- электронной подписи RSA совместно с секретной передачей сообщения;
- аутентификации;
- электронной подписи "вслепую".

---

## ТЕОРЕТИЧЕСКИЕ СВЕДЕНИЯ

> *Если видишь салат и приправу, сразу ясно, какое готовится блюдо.*
> *А.Конан Дойл. Подвиги бригадира Жерара*

### Основные понятия

С развитием электронной почты с компьютера на компьютер стало поступать огромное количество посланий самого разного свойства. Среди личных посланий, поздравлений, рекламных проспектов и прочего в электронной почте стали попадаться и *документы*.

Напомним, что обычный текст превращается в документ, когда под ними появляется *подпись*, и к этому нас приучил многовековой опыт "бумажного" обращения с текстами. Более того, даже один и тот же текст может иметь совершенно разный смысл в зависимости от того, кто его подписал. Одно дело, если текст подписал министр, и совсем другое дело, если тот же самый текст подписал третий секретарь четвёртого отдела министерства.

Однако если имеется подпись, то имеется и проблема борьбы с её подделкой, а переход к электронным документам эту проблему только обострил. Поскольку, например, передача по сети сканированного документа, снабжённого ручной подписью, проблемы подделки ни в коем случае не решает, а для любой серьёзной экспертизы подлинности подписи требуется оригинал "бумажного" документа.

Так возникла задача создания *электронной цифровой подписи*.

**Определение**.  
*Электронной цифровой подписью* (ЭЦП) называется реквизит электронного документа, появление которого в документе получателем подтверждает два факта:
1. документ поступил к получателю без искажений;
2. документ подписан именно отправителем.

Термин "электронная подпись" более привычен для России, хотя в последнее время всё чаще используется принятый в других странах (прежде всего в США) термин "цифровая подпись", что отражено в новых российских стандартах. Другими словами, эти термины являются синонимами.

Ниже будем различать следующие *типы подписей*:
- рукописная личная подпись;
- рукописная нотариальная подпись, когда специально выделяемое лицо (нотариус) заверяет документы своей подписью и печатью, так что любое другое лицо может удостовериться в их подлинности;
- электронная (личная) подпись;
- электронная нотариальная подпись (сертифицированная подпись), которая является аналогом в информационном пространстве рукописной нотариальной подписи.

Сформулируем *свойства*, которым (в идеале) должна удовлетворять подпись любого выделенного типа:
1. подписать документ может только "законный" владелец подписи, т.е. никто не может подделать подпись;
2. автор подписи не может от неё отказаться;
3. в случае возникновения спора возможно участие *третьих лиц* (либо людей, либо информационных систем) для установления подлинности подписи.

Разумеется, цифровая (электронная) подпись также должна обладать всеми этими свойствами, однако лица, подписывающие документы и проверяющие их подлинность, могут находиться за тысячи километров друг от друга и взаимодействовать только через компьютерную сеть.

**Замечания (важные)**:
1. На практике *цифровой сертификат*, содержащий цифровую подпись, выпускается *сертифицирующим органом*, устанавливающим связь между *открытым ключом* и личностью пользователя.
2. Нотариальная подпись может также являться цифровой подписью.

Общепризнанная *схема* (модель) *цифровой подписи* содержит три алгоритмических процесса:
1. генерация ключей (подписи и проверки);
2. формирование подписи;
3. проверка подписи.

---

## Электронная подпись на базе RSA

> *- А когда компьютер уже проанализировал ДНК, как вы определяете, какому животному принадлежит генетический материал?*  
> *- Можно просто вырастить животное и опознать его по внешним признакам, - сказал он. - Обычно мы так поступаем.*  
> *М.Крайтон. Парк юрского периода*

### 1. Электронная подпись RSA без секретной передачи сообщения

Рассмотрим протокол формирования электронной цифровой подписи, основанный на алгоритме RSA. В обмене участвуют отправитель A и получатель B. Пусть A отправляет B текст x и желает, чтобы это сообщение было снабжено электронной цифровой подписью.

*Протокол*, решающий указанную задачу, состоит из следующих шагов [Музыкантский, Фурин, 2011, с.32]:

1. A строит стандартную схему RSA; пусть её *открытый ключ* $(m, e)$ и *закрытый ключ* $d$; как всегда, открытый ключ публикуется, закрытый остаётся известен только A.
2. Если x — отправляемый текст, то A вычисляет с помощью *закрытого ключа* число s по формуле
   $$s = x^d \bmod m$$
   и отправляет получателю B текст x и число s в качестве его электронной цифровой подписи, т.е. пару $(x, s)$.
3. Абонент B, получив пару $(x, s)$, выбирает число s, берёт опубликованный открытый ключ A и вычисляет число
   $$x' = s^e \bmod m.$$
   Теперь если $x' = x$, то подпись правильна, что означает, что исходный текст (действительно подписанный A) принят без искажений. Если же $x' \ne x$, то это означает, что либо в исходный текст внесены изменения, либо он подписан не отправителем A, либо и то, и другое выполнено совместно.

Смысл указанного протокола в том, что абонент B (как и в случае протокола аутентификации) убеждается, что подпись s, полученная им вместе с исходным текстом x, вычислялась с использованием закрытого ключа $(m, d)$, "парного" к открытому ключу отправителя. Такую операцию мог проделать только сам отправитель A, поскольку только он знает закрытый ключ.

> **Внимание!** Получатель не получает никакой информации о самом закрытом ключе отправителя, т.е. в этом протоколе работает *схема нулевого разглашения*.

Схема электронной цифровой подписи имеет дополнительно следующие важные *свойства*:

1. Данная конкретная подпись может относиться только к одному конкретному тексту. Действительно, если отправитель A попытается подобрать другой текст $x'$ так, чтобы он имел ту же подпись, что и исходный текст x, то ему придётся найти новый текст $x'$ из уравнения
   $$(x')^d \bmod m = x^d \bmod m,$$
   а такое уравнение (по крайней мере, в классе осмысленных текстов) может иметь только одно решение. Это обстоятельство определяет важнейшее свойство цифровой подписи — *невозможность отправителя подменить подписанный им текст либо отказаться от подписи*. Это свойство, называемое *неотказуемостью* (неотвергаемостью) *подписи*, ставит в жёсткие рамки отправителя сообщения, в то же время получатель застрахован от возможных последствий недобросовестности отправителя.
2. Отправитель может быть уверен, что его цифровую подпись никто не может подделать, поскольку закрытый ключ знает только он сам.

Эти два обстоятельства делают тексты, подписанные с помощью цифровой подписи, *юридически значимыми*, т.е. они могут служить основой сделок, контрактов, могут приниматься в судах в качестве вещественных доказательств и, вообще, могут признаваться там, тогда и в той степени, где, когда и в какой степени могут признаваться собственноручные *личные подписи*.

### 2. Электронная подпись RSA без секретной передачи сообщения (с хэшированием)

Если Алиса планирует подписывать документы, то она должна вначале выбрать параметры RSA:

1. Алиса выбирает два больших простых числа P, Q и вычисляет
   $$N = P \cdot Q, \quad \varphi(N) = (P-1) \cdot (Q-1).$$
2. Она выбирает число $d$, взаимно простое с $\varphi(N)$, и вычисляет
   $$e = d^{-1} \bmod \varphi(N).$$
3. Она публикует числа N и $d$, например, помещает их на своём сайте, связанном со своим именем, и хранит в секрете число $e$.

Теперь Алиса готова ставить свои подписи на документах или сообщениях. Пусть Алиса хочет подписать сообщение $m = m_1 m_2 \dots m_n$.

Опишем *протокол электронной подписи* [Рябко, Фионов, 2018, с.52-53]:

Вначале Алиса вычисляет значение *хэш-функции*
$$y = h(m_1, m_2, \dots, m_n),$$
которая ставит в соответствие сообщению $m$ число y.

> Пусть алгоритм вычисления хэш-функции известен всем.

Отметим здесь только наиболее важное для нас свойство: практически невозможно изменить текст сообщения $m$, не изменив при этом y. Поэтому на следующем шаге Алисе достаточно снабдить подписью только число y, и подпись будет относиться ко всему сообщению $m$.

Алиса вычисляет число
$$s = y^e \bmod N,$$
т.е. она возводит число y в свою "секретную степень". Число s и есть *цифровая подпись*, которая просто добавляется к сообщению $m$, и этим Алиса формирует *подписанное сообщение* $(m, s)$.

Теперь каждый, кто знает открытые параметры Алисы, связанные с её именем, т.е. числа N и $d$, может проверить подлинность её подписи. Для этого необходимо, взяв подписанное сообщение $(m, s)$:
- вычислить значение хэш-функции $h(m)$;
- вычислить $w = s^d \bmod N$;
- проверить выполнение равенства $w = h(m)$.

**Теорема** [Рябко, Фионов, 2018, с.53]. Если подпись подлинная, то $w = h(m)$.

*Доказательство*. Выполним простые вычисления:
$$w = s^d \bmod N = (y^e \bmod N)^d \bmod N = y^{ed} \bmod N = y = h(m).$$

**Теорема** [Рябко, Фионов, 2018, с.53]. Описанная электронная подпись удовлетворяет всем требованиям, предъявляемым к подписи.

*Доказательство*. Последовательно проверим все свойства подписи:

1. Никто не может разложить число N на простые множители (при больших N порядка 1024 битов *по состоянию на* 2010 эта задача практически неразрешима). Поэтому, зная N и $d$, невозможно найти $e$. Действительно, чтобы вычислить $e = d^{-1} \bmod \varphi(N)$, нужно знать $\varphi(N)$, а для этого нужно знать простые множители P и Q. Итак, никто, кроме Алисы, не может знать число $e$ и поэтому не может подписать сообщение.
2. Автор подписи не может от неё отказаться, т.к. никто другой не может "сфабриковать" подпись от её имени.
3. В случае спора заинтересованная сторона может предъявить судье все вычисления для их проверки и выяснения истины.

**Пример** [Рябко, Фионов, 2018, с.53-54].  
Пусть $P = 5$, $Q = 11$. Тогда $N = 5 \cdot 11 = 55$, $\varphi(N) = 4 \cdot 10 = 40$. Пусть $d = 3$. Такой выбор $d$ возможен, т.к. $\gcd(40,3) = 1$. Параметр $e = 3^{-1} \bmod 40$ вычисляем с помощью обобщённого алгоритма Евклида, $e = 27$.

Пусть Алиса хочет подписать сообщение $m = \text{"abbbaa"}$, для которого значение хэш-функции равно, например, 13, т.е. $h(m) = 13$ (это лишь предположение!). Далее, Алиса вычисляет $s = 13^{27} \bmod 55 = 7$ и формирует подписанное сообщение $(\text{"abbbaa"}, 7)$.

Теперь знающий открытые ключи Алисы $N = 55$ и $d = 3$ может проверить подлинность подписи следующим образом:
- получив подписанное сообщение, он вычисляет значение хэш-функции $h(\text{"abbbaa"}) = 13$ (если содержание сообщения не изменено, то значение хэш-функции совпадёт с тем, которое ранее вычислила Алиса);
- далее, он вычисляет $w = 7^3 \bmod 55 = 13$ и определяет выполнение равенства $h(\text{"abbbaa"}) = w$, т.е. что подпись верна.

### 3. RSA-схема шифрования и подписи

> *- По-моему, мы не то делаем, — сказал вдруг Бильбо, доставая из кармана огромный ключ (для тролля это наверняка был не ключ, а ключик). — Я нашел его у костра. Наверно, выпал у кого-то из кармана.*  
> *- Что же ты молчал? — закричали все хором.*  
> *Гэндальф схватил ключ и вставил в замочную скважину. Ключ повернулся, гномы вновь навалились на плиту, и та отошла в сторону.*  
> *Дж. Толкин. Хоббит, или Туда и Обратно*

Существует способ передачи аутентифицированного сообщения от A к B способом, который называется *подписать и зашифровать*:

1. Абонент A ставит на данные цифровую подпись с помощью закрытого ключа.
2. Абонент A присоединяет подпись к данным.
3. Абонент A шифрует данные и подпись с помощью открытого ключа абонента B.
4. Абонент B расшифровывает данные и подпись с помощью своего закрытого ключа.
5. Абонент B расшифровывает данные и подпись с помощью открытого ключа абонента A, т.е. B уверен, что сообщение пришло именно от абонента A.

Пусть Алиса хочет подписать сообщение m Бобу; при этом Алиса сначала подписывает m с помощью своего секретного ключа, а затем шифрует результат с помощью открытого ключа Боба. Тем не менее, этот алгоритм *не всегда применим* в системе RSA.

Пусть ключами Алисы являются $(n_A, e_A)$, $d_A$, а Боба — $(n_B, e_B)$, $d_B$; при этом важно, что
$$n_A < n_B - 1.$$

Протокол описывается следующим образом:
1. Алиса посылает Бобу:
   $$C = \left( m^{d_A} \bmod n_A \right)^{e_B} \bmod n_B.$$
2. Боб восстанавливает m:
   $$m = \left( C^{d_B} \bmod n_B \right)^{e_A} \bmod n_A.$$

Однако возникает новая проблема, а именно: что делать, если Боб захочет подписать конфиденциальное сообщение Алисе? Простое решение состоит в том, чтобы каждый пользователь создавал два набора параметров: один с модулем, меньшим некоторого порога $T \in \mathbb{N}$, а другой — с модулем, большим T. В этих условиях отправитель использует свой собственный меньший модуль для подписи и больший модуль получателя — для шифрования.

Если между Алисой и Бобом возникает спор, то они идут к арбитру, который получает от Боба числа
$$u = C^{d_B} \bmod n_B, \quad \text{а также } m.$$
Легко видеть, что
$$u = \left( \left( m^{d_A} \bmod n_A \right)^{e_B} \bmod n_B \right)^{d_B} = m^{d_A} \bmod n_A,$$
поэтому, если теперь
$$u^{e_A} \equiv m \pmod{n_A},$$
то сообщение m действительно пришло от Алисы; если же нет, то u не будет рассматриваться как подпись Алисы к m.

Заметим, что арбитр не нуждается в знании закрытых ключей Алисы или Боба. Поэтому Алиса и Боб могут продолжать их использовать.

### 4. Протокол подписи "вслепую"

Рассмотрим протокол, который называется *подпись "вслепую"* (англ. *blind signature*). При использовании этой криптографической схемы отправитель, подписывающий сообщение, *не знает* содержания сообщения.

Пусть, следуя схеме RSA, найдены:
- $d$ — закрытый ключ создания подписи,
- $(n, e)$ — открытый ключ проверки подписи.

Обозначим m — сообщение, для которого необходимо получить подпись, не раскрывая его содержания, а в создании подписи участвуют и отправитель, подписывающий сообщение, и получатель этого сообщения.

Получатель генерирует случайное число r такое, что $(r, n) = 1$, и посылает отправителю вычисленное с помощью открытого ключа значение
$$m' = r^e \cdot m \pmod n.$$
Очевидно, что содержание сообщения m "скрыто" числом r.

Отправитель возвращает подписанное сообщение (он обязан это выполнить по условию):
$$s' = (r^e \cdot m)^d \pmod n = r^{ed} \cdot m^d \pmod n = r \cdot m^d \pmod n.$$
В самом деле, вспомним, что $d \cdot e \equiv 1 \pmod{\varphi(n)}$ и $d \cdot e = 1 + k\varphi(n)$, $k \in \mathbb{Z}$, а по построению схемы $(r, n) = 1$; тогда по теореме Эйлера
$$r^{ed} = r^{1 + k\varphi(n)} = r \cdot (r^{\varphi(n)})^k \equiv r \pmod n.$$

Получатель вычисляет значение
$$s = s' \cdot r^{-1} \pmod n = (r \cdot m^d) \cdot r^{-1} \pmod n = m^d \pmod n,$$
которое и является значением подписи.

Подпись считается подлинной, если выполнено сравнение
$$s^e \equiv m \pmod n.$$
В самом деле,
$$s = m^d \pmod n \Rightarrow s^e = m^{de} \pmod n = m \pmod n.$$

---

## Аутентификация

### 1. Протокол аутентификации

> *Между домами устроены магнетические телеграфы, посредством которых живущие на далёком расстоянии общаются друг с другом.*  
> *В.Ф.Одоевский. 4338-й год*

Базы данных компьютеров хранят огромное количество информации. Часть её общедоступна и с ней может ознакомиться любой желающий. Однако кроме общедоступной информации в базах данных может также храниться информация, представляющая служебную, коммерческую или государственную тайну. Ясно, что эта информация не может быть общедоступной, она является закрытой. Но также ясно, что если закрытая информация имеется, значит, кто-то должен иметь к ней доступ, даже к самой секретной.

**Определение**.  
*Разграничением доступа* называется процесс разделения всех пользователей на категории в зависимости от вида доступной информации.

К этому понятию относят также и способ обработки информации, доступный тому или иному пользователю — одному пользователю разрешается один фрагмент информации только читать, а другой фрагмент ещё и изменять, либо совсем уничтожать, либо создавать новый фрагмент информации. А другой пользователь может всю информацию использовать только в режиме чтения.

Короче говоря, концепция разграничения доступа предусматривает следующее *условие*: прежде чем допустить какого-либо пользователя к информации, необходимо определить его полномочия, а для этого нужно сначала этого пользователя *идентифицировать*.

**Определение**.  
1. *Идентификация* — это процесс установления личности человека или идентичности юридического лица. Идентификация базируется на одном из следующих признаков или их комбинации:
   - предъявлении документа (например, магнитной карты, смарт-карты, паспорта);
   - знании определённого "текста" (например, пароля, PIN, секретного ключа);
   - проверке индивидуальных особенностей (например, отпечатков пальцев, подписи, голоса, структуры сетчатки глаза, ДНК и т.п.);
   - демонстрации определённых умений (например, умение доказать некоторый математический результат).
2. [Словарь,2006] *Идентификация* — это процедура установления присвоенного данной стороне уникального системного имени — *идентификатора*, которое позволяет отличать её от других сторон. Обычно процедура идентификации заключается в предъявлении этого имени и предшествует процедуре *аутентификации*, т.е. подтверждению правильности идентификации. Термин "идентификация" часто для краткости используют для обозначения общей процедуры идентификации/аутентификации сторон.

**Определение**.  
1. *Аутентификацией* называется криптографический *процесс идентификации пользователя*, защищающий информационную систему от несанкционированного доступа.
2. *Аутентификацией* называется процедура, с помощью которой проверяющий устанавливает законность *идентификации* пользователя в информационной системе. Результатом аутентификации является принятие или отклонение поступившего от пользователя требования. Другими словами, пользователь должен доказать свою правомочность, а проверяющий — проверить правильность доказательства.

**Замечания (важные)**:
1. Процессы идентификации и аутентификации различны (*почему?*).
2. Существует связь протокола аутентификации с протоколом цифровой подписи, однако главным отличием является то, что цифровая подпись, как правило, не связана с реальным временем.

Рассмотрим *протокол аутентификации*, который основан на алгоритме RSA [Музыкантский, Фурин, 2011, с.30] и в котором участвуют два пользователя: A — пользователь, пытающийся пройти аутентификацию, B — проверяющий.

Протокол состоит из следующих шагов:

1. Проверяемый A строит стандартную схему RSA; пусть её *открытый ключ* $(m, e)$ и *закрытый ключ* $(m, d)$ (естественно, что открытый ключ публикуется, а закрытый остаётся известен только A).
2. Проверяющий B посылает проверяемому A случайную строку s.
3. Проверяемый A вычисляет с помощью известного ему закрытого ключа d число g по формуле
   $$g = s^d \bmod m$$
   и отправляет полученное число g проверяющему B.
4. Проверяющий B, получив число g, выбирает из публикации открытый ключ абонента A и вычисляет с его помощью число
   $$s' = g^e \bmod m.$$
   Если при этом $s' = s$, то аутентификация *принята*, если же $s' \ne s$, то аутентификация *отвергнута*.

Смысл указанного протокола в том, что проверяющий B убеждается в том, что абонент, проходящий процедуру аутентификации, действительно знает закрытый ключ системы RSA, "парный" открытому ключу абонента A, а значит, он является тем самым проверяемым A.

Любой другой абонент, пытающийся выдать себя за абонента A с целью получить его полномочия по доступу к информационной системе, должен для успешного прохождения процедуры идентификации:
1. либо взломать схему RSA, построенную абонентом A, что практически невозможно при условии грамотного построения абонентом схемы RSA;
2. либо просто украсть у A его закрытый ключ d, что практически невозможно при условии грамотного соблюдения абонентом A правил хранения секретной информации.

Выполнение обоих этих условий возлагается на абонента A, поскольку это целиком в его интересах.

> **Внимание!** При использовании указанного протокола аутентификации закрытый ключ d пользователя A не раскрывался.

Отметим, что выше мы рассмотрели только простейший случай протокола аутентификации. Для подтверждения наших слов приведём

**Определение** [Словарь,2006] (важное):
1. *Протокол аутентификации сообщения* — это криптографический протокол, предназначенный для обеспеченности целостности сообщений, под которым понимается гарантируемая получателю возможность удостовериться, что сообщение поступило от заявленного отправителя и в неискажённом виде. В случае, когда участники протокола доверяют друг другу и защищаются от противника, протоколы аутентификации сообщений строятся на основе *кодов аутентичности сообщений*. Если же участники друг другу не доверяют, то для аутентификации сообщений используется *схема цифровой подписи*.
2. *Код аутентичности сообщения* (синоним: *имитовставка*) — это специальный набор символов в протоколах аутентификации сообщений с доверяющими друг другу участниками, добавляемый к сообщению и предназначенный для обеспечения его целостности и аутентификации источника данных.
3. *Протокол идентификации* — это протокол аутентификации сторон, участвующих во взаимодействии и не доверяющих друг другу. Различают протоколы идентификации:
   - с односторонней аутентификацией;
   - со взаимной аутентификацией.
   Протоколы идентификации, как правило, основаны на известной обеим сторонам информации (пароли, личные идентификационные номера, секретные ключи, открытые ключи) и реализуются с использованием техники "запрос-ответ" или *доказательства с нулевым разглашением*. В дополнении к протоколу идентификации могут использоваться некоторые физические приборы, с помощью которых проводится идентификация (магнитная или интеллектуальная пластиковая карта, прибор, генерирующий меняющиеся со временем пароли), а также биометрические параметры.

**Замечания (для знатоков)**:
1. Различают *слабую* (пароли, личные идентификационные номера (PIN)) и *сильную* аутентификацию.
2. Протокол аутентификации иллюстрирует одно из важных понятий математической криптографии — *доказательство с нулевым разглашением*.

### 2. Закрытый информационный обмен между двумя абонентами

> *Не выходи из комнаты, не совершай ошибку.*  
> *Зачем тебе Солнце, если ты куришь "Шипку"?*  
> *За дверью бессмысленно всё, особенно — возглас счастья.*  
> *И.Бродский. Не выходи из комнаты... (1970)*

Криптографические протоколы, часть которых приведена в предыдущем разделе, позволили найти решение многих новых задач, в том числе и тех, которые раньше вообще считались неразрешимыми (например, *задача о подбрасывании монеты по телефону*). Более того, криптографические протоколы стали основой развития многих новых направлений информатики, из которых следует в первую очередь отметить *информационную безопасность*.

Однако сами по себе криптографические протоколы только предоставляют математическую основу решения проблемы. Они вполне достаточны для организации защищённого компьютерного обмена между двумя абонентами.

Пусть, например, два абонента хотят наладить между собой компьютерный обмен так, чтобы гарантировать его конфиденциальность, даже в случае вторжения активных противников. Криптографические протоколы предоставляют для решения подобной задачи разнообразные *инструменты*, в частности, они могут (по [Музыкантский, Фурин, 2011, с.42-43]):
1. договориться об использовании в процессе обмена взаимную *аутентификацию*;
2. *шифровать* свои послания;
3. договориться об использовании в процессе обмена *электронной цифровой подписи*. При этом они могут в зависимости от взаимных договорённостей подписывать послания, содержащие либо открытые, либо уже зашифрованные тексты;
4. шифровать саму (!) электронную подпись.

Для реализации этого многообразия возможных вариантов защиты партнёрам достаточно завести каждому по три пары открытых и закрытых ключей (по одной паре для каждой из операций: *аутентификации*, *шифрования* и *подписи*) и обменяться открытыми ключами.

**Пример** [Музыкантский, Фурин, 2011, с.43].  
Пусть два партнёра желают зашифровать свой компьютерный обмен. Тогда они могут действовать следующим образом:

1. A и B выбирают общий алгоритм шифрования E и соответствующий ему алгоритм расшифрования D.
2. A выбирает пару ключей $k_1$ и $k_2$; при этом открытый ключ $k_1$ он сообщает партнёру B, закрытый ключ $k_2$ остаётся известен только ему.
3. Аналогично, B выбирает пару ключей $q_1$ и $q_2$, а открытый ключ $q_1$ сообщает своему партнёру A.
4. Перед отправлением от A к B сообщение x шифруется отправителем с использованием известного ему открытого ключа $q_1$ получателя B и полученный зашифрованный текст $E_{q_1}(x)$ пересылается получателю.
5. B расшифровывает полученный текст $E_{q_1}(x)$ с использованием своего закрытого ключа $q_2$, известного только ему: $D_{q_2}(E_{q_1}(x)) = x$.
6. Аналогично сообщение от B к A сначала шифруется отправителем B с использованием открытого ключа $k_1$ получателя A, и зашифрованный текст $E_{k_1}(x)$ пересылается A, который расшифровывает его с использованием своего закрытого ключа, т.е. $D_{k_2}(E_{k_1}(x)) = x$.

Примерно по такой же схеме партнёры могут организовать процедуру электронной цифровой подписи для своих сообщений. При этом отправитель A должен организовать вычисление цифровой подписи своего сообщения с использованием своего закрытого ключа, а получатель B организует проверку полученной подписи с использованием заранее ему известного открытого ключа отправителя A.

Всё будет работать до тех пор, пока один из партнёров:
1. не утратит свой закрытый ключ;
2. закрытый ключ не станет известен каким-либо третьим лицам.

---

## ПРИМЕРЫ РЕШЕНИЯ НЕКОТОРЫХ ТИПОВ УПРАЖНЕНИЙ

> *Теории приходят и уходят, а примеры остаются.*  
> *И.М.Гельфанд*

### Пример 1

(По [Нечаев,1999,с.32-33]) Приведите пример "ручной" реализации протокола *электронной подписи для двух абонентов*.

*Решение*. Пусть банкир В выбирает простые числа 7 и 13, вкладчик W выбирает простые числа 11 и 23. Таким образом,
$$R = 7 \cdot 13 = 91, \quad r = 11 \cdot 23 = 253.$$

Пусть $S = 5$ — *открытый ключ банкира*, а $s = 31$ — *открытый ключ вкладчика*. Далее, вычислим *закрытый ключ банкира*
$$5 \cdot T \equiv 1 \pmod{72} \Rightarrow T = 29,$$
и *закрытый ключ вкладчика*:
$$31 \cdot t \equiv 1 \pmod{220} \Rightarrow t = 71.$$

Тогда *открытая телефонная книга* имеет вид
|        | В      | W       |
|--------|--------|---------|
| Ключи  | R=91, S=5 | r=253, s=31 |

Важно отметить, что $R < r$.

Вкладчик W даёт поручение $m = 41$ банкиру В и, замечая, что $R < r$, шифрует его вначале открытым ключом банкира, а затем своим секретным ключом:
$$m_1 = 41^5 \bmod 91 = 6, \quad m_2 = 6^{71} \bmod 253 = 94.$$

Банкир, получив шифрограмму $m_2 = 94$ и замечая, что $R < r$, расшифровывает её, пользуясь сначала открытым ключом вкладчика, а затем своим закрытым ключом:
$$m_3 = 94^{31} \bmod 253 = 6, \quad m_4 = 6^{29} \bmod 91 = 41.$$

Так как $41 < 91$, то банкир делает вывод, что 41 и есть распоряжение этого вкладчика.

---

## ЗАДАЧИ ДЛЯ САМОСТОЯТЕЛЬНОГО РЕШЕНИЯ

> *Они вчетвером держали изящную машину, покуда я взбирался на седло, потом строились колонной и маршировали по обеим сторонам, а инструктор подталкивал меня сзади.*  
> *М.Твен. Укрощение велосипеда*

### 1. Криптографические протоколы

**Указание (важное)**. В задачах № 1-2 будем предполагать, что $h(m) \equiv m$ для всех значений m.

1. [Рябко, Фионов, 2018, с.62, №4.1] Постройте подпись RSA для сообщения m при следующих параметрах пользователя:
   - (а) $P = 5$, $Q = 11$, $e = 27$, $m = 7$;
   - (б) $P = 5$, $Q = 13$, $e = 29$, $m = 10$;
   - (в) $P = 7$, $Q = 11$, $e = 43$, $m = 5$;
   - (г) $P = 7$, $Q = 13$, $e = 29$, $m = 15$;
   - (д) $P = 3$, $Q = 11$, $e = 7$, $m = 24$.

2. [Рябко, Фионов, 2018, с.62, №4.2] Для указанных открытых ключей пользователя RSA проверьте подлинность подписанных сообщений:
   - (а) $N = 55$, $d = 3$: $(7,28)$, $(22,15)$, $(16,36)$;
   - (б) $N = 65$, $d = 5$: $(6,42)$, $(10,30)$, $(6,41)$;
   - (в) $N = 77$, $d = 7$: $(13,31)$, $(11,28)$, $(5,26)$;
   - (г) $N = 91$, $d = 5$: $(15,71)$, $(11,46)$, $(16,74)$;
   - (д) $N = 33$, $d = 3$: $(10,14)$, $(24,18)$, $(17,8)$.

### 2. Задачи на программирование

1. Реализуйте с помощью системы символьных вычислений Maple протоколы:
   - (1) электронной подписи RSA без секретной передачи сообщения;
   - (2) электронной подписи RSA без секретной передачи сообщения (с хэшированием);
   - (3) электронной подписи RSA совместно с секретной передачей сообщения;
   - (4) аутентификации;
   - (5) подписи "вслепую" (по [Ростовцев, Маховенко, 2001, с.192-193]).

2. [Рябко, Фионов, 2018, с.63, №4.7] Разработайте программы для генерации и проверки подписей RSA. Параметры пользователей выберите *самостоятельно*. Для тестирования программы проверки подписи рекомендуется использовать подписанное сообщение $\langle 500, 46514 \rangle$ для открытых ключей пользователя $N = 52891$, $d = 3$. Предполагаем, что $h(m) \equiv m$ для всех значений m. Данное сообщение должно признаваться истинным. Любое изменение подписанного сообщения *с большой вероятностью* будет делать подпись недействительной.

3. (По [Шнайер, 2002, с.558]) Реализуйте схему ЭЦП *Онга-Шнорра-Шамира*. Пусть n — большое нечётное число. Выбирается случайное число k такое, что $(n, k) = 1$, и вычисляется
   $$h = -\frac{1}{4k^2} \pmod n = -\left(\frac{1}{4k}\right)^2 \pmod n.$$
   В этой схеме $(n, h)$ — открытый ключ, k — закрытый ключ.

   *Протокол подписи* сообщения $M < n$ таков:
   - выберем случайное число r, взаимно простое с n;
   - вычислим
     $$s_1 = \frac{1}{2} \left( \frac{M}{r} + r \right) \pmod n, \quad s_2 = \frac{k}{2} \left( \frac{M}{r} - r \right) \pmod n;$$
   - формируем подписанное сообщение $(M, s_1, s_2)$.

   *Протокол проверки подписи* прост:
   - вычислим $u = (s_1^2 + h \cdot s_2^2) \pmod n$;
   - если $u = M$, то подпись подтверждена.

   Доказательство правильности протокола:
   $$s_1^2 + h \cdot s_2^2 = \frac{1}{4} \left( \frac{M}{r} + r \right)^2 - \frac{1}{4k^2} \cdot \frac{k^2}{4} \left( \frac{M}{r} - r \right)^2 = \frac{1}{4} \left( \frac{M^2}{r^2} + 2M + r^2 \right) - \frac{1}{16} \left( \frac{M^2}{r^2} - 2M + r^2 \right) = M.$$

---

*Домашнее задание*:  
№ 2.1(1,2,3,4,5) (5 задач).
